import styled from "@emotion/styled";
import { NavLink } from "react-router-dom";

const LinkStyled = styled(NavLink)`
	padding: 10px;
`;
const SubLinkStyled = styled(NavLink)``;

export { LinkStyled, SubLinkStyled };
